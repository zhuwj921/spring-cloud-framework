server:
  port: 8910
spring:
  application:
    name: service-auth
  profiles:
    active: dev
feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: basic

mybatis-plus:
  mapper-locations: classpath:mapper/*Mapper.xml
